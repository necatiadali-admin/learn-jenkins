pipeline {
    agent any
    stages{
        stage('Initialize'){
            steps{
                echo 'React App'
            }
        }
        stage('npm install'){
            steps{
                sh 'npm install'
            }
        }
    }
}